"""Provider adapter layer.

Provides a stable boundary between internal domain models and external
tournament data sources. The rest of the application must never depend on
provider-specific data formats directly.
"""
