# Database Design

Status: Initial schema migration reconciled; targeted contract decisions applied.
Last reconciled: 2026-06-29.

This document remains the target database design. It now also records how the
implemented Alembic migration aligns with the target model and which invariants
are currently enforced in backend services rather than in PostgreSQL.

## Goals

The PostgreSQL model supports private pools, users, knockout bracket matches, predictions, scoring, and rankings. The backend is the only application component that connects to the database.

All schema changes must be implemented through migrations. The recommended migration tool is Alembic.

The repository currently contains an initial Alembic migration at
`app/db/migrations/versions/20260628_0001_initial_backend_schema.py`.

## Ownership

The backend owns all database access. Frontend services must never connect directly to PostgreSQL or receive `DATABASE_URL`.

Repositories are the only backend modules allowed to express SQL persistence details. Services may coordinate repositories, but should not spread ad hoc query logic through API routers.

## Data Model Summary

```text
users 1--* oauth_accounts
users 1--* pool_participants
tournaments 1--* teams
tournaments 1--* matches
tournaments 1--* pools
pools 1--* pool_participants
pools 1--* predictions
matches 1--* predictions
predictions 1--0..1 prediction_scores
matches 0..1--0..1 matches via next_match_id
```

Important modeling decisions:

- Predictions belong to a pool, not only to a user and match.
- Rankings are derived from prediction scores.
- Invite codes, refresh tokens, and exchange codes are stored only as hashes.
- Bracket progression is explicit through `matches.next_match_id` and `matches.next_match_slot`.

## Current Implementation Snapshot

The initial migration creates the core MVP schema:

- PostgreSQL extensions: `pgcrypto` and `citext`.
- Enums: `tournament_stage`, `match_status`, `pool_role`, `participant_status`, `next_slot`, and `prediction_status`.
- Tables: `users`, `oauth_accounts`, `tournaments`, `teams`, `matches`, `pools`, `pool_participants`, `predictions`, `prediction_scores`, `auth_sessions`, and `auth_exchange_codes`.
- Indexes/constraints for unique emails, OAuth subjects, tournament/team/match identity, invite-code hashes, participant uniqueness, prediction uniqueness, non-negative scores/predictions, and auth token/code hashes.

The implemented ORM models live under `app/models`, and repository access lives
under `app/repositories`. This matches the intended backend-only database
ownership boundary.

## Reconciliation Gaps

The following target invariants are not fully enforced by the initial migration
and should be reviewed by Backend/Reviewer before production:

- `matches`: the migration enforces non-negative scores and distinct teams, but does not enforce "completed matches have scores and a winner", "final matches cannot have `next_match_id`", or "`next_match_slot` must be present when `next_match_id` is present". Some of this is currently enforced by `AdminService`.
- `teams`: the documented unique `(tournament_id, fifa_code)` constraint is implemented as a normal unique constraint, not a partial unique index excluding null `fifa_code` values. PostgreSQL allows multiple nulls in a unique constraint, so behavior is acceptable, but the implementation detail differs from the wording.
- `predictions`: pool/match tournament consistency and active pool membership are enforced in the service layer, not by composite foreign keys or database constraints.
- `prediction_scores`: rankings are derived from queries, not a database view. That is acceptable for MVP, but the docs should keep calling it derived rather than a stored ranking table.

No schema change is made by this reconciliation. These gaps should become
Backend-owned tasks if the Reviewer decides database-level enforcement is
required for merge or production readiness.

## Conventions

- Primary keys use UUIDs.
- Timestamps use `timestamptz` and are stored in UTC.
- Tables include `created_at` and `updated_at` where records are mutable.
- Foreign keys are explicit.
- Constraints protect business invariants where practical.
- Repositories own SQL access.
- Rankings are derived from scores and should not be manually edited.

## Enums

Recommended PostgreSQL enum values:

```text
tournament_stage:
  round_of_32
  round_of_16
  quarter_final
  semi_final
  final

match_status:
  scheduled
  locked
  in_progress
  completed
  cancelled

pool_role:
  owner
  participant

participant_status:
  active
  removed

next_slot:
  home
  away

prediction_status:
  editable
  locked
  scored
```

## Tables

### users

Stores application users.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk | Generated by application or database |
| email | citext unique not null | Case-insensitive email |
| display_name | text not null | User-facing name |
| avatar_url | text nullable | From Google profile |
| is_active | boolean not null default true | Soft account disable |
| created_at | timestamptz not null | UTC |
| updated_at | timestamptz not null | UTC |

Indexes:

- Unique index on `email`.

### oauth_accounts

Links users to OAuth providers.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| user_id | uuid fk users(id) not null | Cascade delete |
| provider | text not null | MVP value: `google` |
| provider_subject | text not null | Google `sub` |
| provider_email | citext not null | Email at login time |
| created_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Constraints:

- Unique `(provider, provider_subject)`.
- Unique `(provider, provider_email)` can be considered, but the stable identity should be provider subject.

### tournaments

Stores a tournament bracket definition.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| name | text not null | Example: `FIFA World Cup 2026` |
| year | integer not null | 2026 |
| is_active | boolean not null default true | Current tournament flag |
| created_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Constraints:

- Unique `(year, name)`.

### teams

Stores teams participating in the knockout bracket.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| tournament_id | uuid fk tournaments(id) not null |  |
| name | text not null | Team name |
| short_name | text nullable | Display abbreviation |
| fifa_code | text nullable | Optional three-letter code |
| flag_url | text nullable | Optional display asset |
| created_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Constraints:

- Unique `(tournament_id, name)`.
- Unique `(tournament_id, fifa_code)` where `fifa_code is not null`.

### matches

Stores knockout matches and bracket progression links.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| tournament_id | uuid fk tournaments(id) not null |  |
| stage | tournament_stage not null |  |
| bracket_position | integer not null | Position inside stage |
| home_team_id | uuid fk teams(id) nullable | Nullable until known |
| away_team_id | uuid fk teams(id) nullable | Nullable until known |
| scheduled_at | timestamptz not null | Kickoff/lock time |
| status | match_status not null default `scheduled` |  |
| home_score | integer nullable | Actual goals |
| away_score | integer nullable | Actual goals |
| winner_team_id | uuid fk teams(id) nullable | Winner after completion |
| next_match_id | uuid fk matches(id) nullable | Where winner advances |
| next_match_slot | next_slot nullable | `home` or `away` |
| created_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Constraints:

- Unique `(tournament_id, stage, bracket_position)`.
- Check scores are null or non-negative.
- Check `home_team_id <> away_team_id` when both are present.
- Check completed matches have scores and a winner.
- Check non-completed matches do not require scores.
- Check `next_match_id is null` for the final.
- Check `next_match_slot is not null` when `next_match_id is not null`.

Indexes:

- `(tournament_id, stage, bracket_position)`.
- `(scheduled_at)`.
- `(status)`.

### pools

Stores private prediction pools.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| tournament_id | uuid fk tournaments(id) not null |  |
| owner_user_id | uuid fk users(id) not null |  |
| name | text not null |  |
| invite_code_hash | text unique not null | Hash of current invite code |
| invite_code_last_rotated_at | timestamptz not null |  |
| is_active | boolean not null default true |  |
| created_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Indexes:

- `(owner_user_id)`.
- `(tournament_id)`.
- Unique `invite_code_hash`.

### pool_participants

Stores membership in private pools.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| pool_id | uuid fk pools(id) not null | Cascade delete |
| user_id | uuid fk users(id) not null | Cascade delete |
| role | pool_role not null default `participant` |  |
| status | participant_status not null default `active` |  |
| joined_at | timestamptz not null |  |
| removed_at | timestamptz nullable |  |

Constraints:

- Unique `(pool_id, user_id)`.
- Pool owner should also have a participant row with role `owner`.

Indexes:

- `(user_id, status)`.
- `(pool_id, status)`.

### predictions

Stores a user's prediction for a match within a specific pool.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| pool_id | uuid fk pools(id) not null |  |
| user_id | uuid fk users(id) not null |  |
| match_id | uuid fk matches(id) not null |  |
| predicted_home_goals | integer not null |  |
| predicted_away_goals | integer not null |  |
| status | prediction_status not null default `editable` | Derived state can be persisted for audit |
| submitted_at | timestamptz not null |  |
| updated_at | timestamptz not null |  |

Constraints:

- Unique `(pool_id, user_id, match_id)`.
- Check predicted goals are non-negative.
- `pool_id` must reference a pool for the same tournament as the match. Enforce in service layer initially; consider a composite FK later if needed.
- User must be an active participant of the pool. Enforce in service layer.

Indexes:

- `(pool_id, match_id)`.
- `(user_id, pool_id)`.
- `(match_id)`.

### prediction_scores

Stores calculated points and scoring details.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| prediction_id | uuid fk predictions(id) unique not null | Cascade delete |
| points | integer not null | Total points |
| correct_winner | boolean not null default false |  |
| exact_score | boolean not null default false |  |
| partial_home_goals | boolean not null default false |  |
| partial_away_goals | boolean not null default false |  |
| scoring_version | text not null | Example: `mvp-2026-v1` |
| calculated_at | timestamptz not null |  |

Constraints:

- Check `points >= 0`.

Indexes:

- `(points)`.

### auth_sessions

Optional table for refresh-token rotation and logout support. For MVP security, this is recommended over stateless refresh tokens only.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| user_id | uuid fk users(id) not null | Cascade delete |
| refresh_token_hash | text unique not null | Never store raw token |
| expires_at | timestamptz not null |  |
| revoked_at | timestamptz nullable |  |
| created_at | timestamptz not null |  |

Indexes:

- `(user_id)`.
- `(expires_at)`.

### auth_exchange_codes

Short-lived one-time codes used to complete backend-owned Google OAuth from the frontend.

| Column | Type | Notes |
| --- | --- | --- |
| id | uuid pk |  |
| code_hash | text unique not null | Never store raw code |
| user_id | uuid fk users(id) not null | Cascade delete |
| redirect_uri | text not null | Expected frontend callback |
| expires_at | timestamptz not null | Usually 1-5 minutes |
| consumed_at | timestamptz nullable | One-time use |
| created_at | timestamptz not null |  |

Indexes:

- `(expires_at)`.

## Derived Rankings

Rankings should be read from a query or database view rather than a manually maintained table:

```text
ranking row:
  pool_id
  user_id
  display_name
  total_points
  exact_scores
  correct_winners
  predictions_scored
  predictions_submitted
  rank
```

Recommended ordering:

1. Total points descending.
2. Exact scores descending.
3. Correct winners descending.
4. Earliest joined participant ascending.

If performance becomes an issue, add a materialized view or cached `pool_rankings` table updated after match scoring. This is not needed for the MVP.

## Scoring Persistence

The scoring engine remains pure domain code. Persistence flow:

1. Admin completes a match by setting actual scores and winner.
2. Prediction service fetches predictions for the match.
3. Domain scoring engine calculates results.
4. Repository upserts `prediction_scores`.
5. Ranking endpoint reads aggregate results.

This design allows recalculating scores after a scoring-rule change by storing `scoring_version`.

## Migration Strategy

Initial migration order, already represented by the current initial migration:

1. Enable required extensions: `pgcrypto` for UUID generation and `citext` for case-insensitive email.
2. Create enums.
3. Create identity tables: `users`, `oauth_accounts`.
4. Create tournament tables: `tournaments`, `teams`, `matches`.
5. Create pool tables: `pools`, `pool_participants`.
6. Create prediction tables: `predictions`, `prediction_scores`.
7. Create auth support tables: `auth_sessions`, `auth_exchange_codes`.
8. Add indexes and optional ranking view.

Production migration rules:

- Migrations must be reviewed before deployment.
- Backward-compatible migrations are preferred.
- Never edit applied migration files.
- Run migrations as a release step before backend deployment or as a controlled Render job.
